# Quota-aware orchestration infrastructure

Infrastructure only; PLAYER-CLOCK-001 is paused. Do not interact with PPSSPP,
change gameplay, or launch substantive research.

Deliver compact AGENTS startup, CURRENT_STATE (preferred <=4096 bytes, maximum
8192), a targeted evidence index, worker-authored compact handoffs with full
reports preserved, deterministic aggregation/status, and detached completion
without parent callbacks. Retain existing CODEX_HOME and persistent Windows
User/Machine key hydration; never print secrets. Preserve legacy mission paths.

Parent policy: GPT-6 Astra, Medium; existing external DeepSeek Explorer/Mapper/
Skeptic only. No native OpenAI research subagents. No repeated parent polling.
Launch broad research early after checkpointing. Read handoff before reports.

Verify syntax, malformed/absent handoffs, partial failures, legacy status and
one cheap detached real DeepSeek self-test (one role, no research). Commit only
infrastructure separately. Record limitations honestly: a detached child
surviving launcher exit demonstrates process independence, not an induced
account-quota exhaustion or survival of OS termination.

Completion report in French: changes, startup reads, omitted bulk reads,
handoffs, quota behavior, exact status command, manual setup, short player-clock
resume prompt. Do not resume gameplay in this task.
