# Final bounded quota-routing update

Owner request: final orchestration refactor before PLAYER-CLOCK-001 resumes.
Parent GPT-6 Astra Medium; optimize validated output per parent quota.

Implement deterministic script -> read-only Luna -> external detached DeepSeek ->
Astra live experiment/arbitration routing. Project roles luna_reader/luna_triage:
GPT-5.6 Luna Medium, read-only, native concurrency maximum2 (prefer1).
Select DeepSeek roles explicitly; preserve existing detached quota failsafe.
Update minimal AGENTS rule and orchestration docs: three additional substantial
reads after startup, no duplicate investigation, results-first, one tooling detour,
read-only existing PPSSPP source cache, no further refactor without experimental need.

Do not launch substantive research, rebuild/reinstall/duplicate PPSSPP or resume
gameplay in this task. Preserve existing experiments and running missions.
Use deterministic local config/parser checks; no paid DeepSeek test needed.
Commit infrastructure separately with exact message:
`infra: route V2 work by quota and task complexity`.
Then STOP. Report in French hierarchy, Luna settings, failsafe, read budget,
manual configuration requirements and a short exact PLAYER-CLOCK-001 resume prompt.

## Completion

Routing docs and project Luna roles implemented; required DeepSeek role selection
replaces automatic trio defaults without changing detachment. Existing orchestration
fixture tests passed, including explicit-role metadata checks. Python tomllib checks
passed for parent/Luna models, Medium effort, read-only sandbox, never approvals,
concurrency2 and relative role references. git diff --check passed. No paid provider
test or substantive research mission launched for this refactor. One read-only Luna
performed only the official configuration-schema lookup.
Reopen a task to load project configuration; no manual file edits required.
