# PLAYER-CLOCK-001 — competing hypotheses

Date: 2026-09-19. Evidence anchor: `../pokitaru/player-clock-socle-map.md` and generated binary extracts. No V2 gameplay claim is TESTED. Arms are defined in `research/tasks/pokitaru-player-clock-001.md`. Predictions assume independently measured B cadence doubles; otherwise compare against measured cadence, not a fixed factor of two.

| ID | Claim and status | Prediction | Falsifier / next discriminating evidence |
|---|---|---|---|
| H-PC-001 | General delta controls Ratchet animation progression; INFERRED weakly from multiple delta consumers, specific ownership UNKNOWN. | B0 shortens idle cycle; B1 restores it even if movement does not match. | B1 leaves phase rate doubled with verified delta delivery and no state/animation change. Trace animation consumer to distinguish direct from mediated effects. |
| H-PC-002 | Repeated player body controls locomotion cadence; INFERRED. | B0 doubles body hits/s; B2 restores hits/s and fixed-step displacement when the loop branch is active. | Relevant motion occurs on alternate branch or body hits are restored but displacement remains wrong. |
| H-PC-003 | Local scalar separately controls jump/trajectory integration; INFERRED, precise units UNKNOWN. | B3 changes trajectory while leaving loop count unchanged; C1 versus D isolates local compensation after delta+substep changes. | Verified scalar change does not affect airborne state or trajectory; another path owns jump. |
| H-PC-004 | Animation is advanced once per substep; UNKNOWN. | B2 restores animation rate; B1 may not. Phase writes/callback count correlate with loop count. | Animation advances outside loop, or B2 normalizes body calls but phase still doubles. |
| H-PC-005 | Coupled outer-rate × substeps × local-delta conserves motion only with combined changes; INFERRED. | D restores relevant additive path better than C1; delta+substeps without local doubling may under-integrate. | Single ablation restores all observables or actual scalar units/call path violate assumed product. Check paths outside loop separately. |
| H-PC-006 | Historical full socle is accidental compensation for some states; UNKNOWN. | D matches straight running but fails acceleration, jump, idle animation or first-substep-only events. | Replicated diverse paths plus dataflow explain every tested observable within predeclared tolerances. This only rejects the hypothesis for those paths. |
| H-PC-007 | WF-002 apparent self-normalization is induced by the player 2→1 patch; INFERRED from history, no causal trace. | Clean B0 doubles relevant WF-002 calls/s; B2 reduces them while holding route vanilla. Call stack links to the loop. | B0 already preserves rate, or a verified non-player caller owns it. |
| H-PC-008 | WF-002 has an independent gating/normalization mechanism; UNKNOWN competing with H-PC-007. | WF-002 rate stays similar A0/B0 despite player loop doubling. | B0 doubles and only substep patch normalizes while all other conditions match. |
| H-PC-009 | Animation has multiple clocks (pose, events, transitions); UNKNOWN. | Idle cycle normalizes in B1 but attack contact/transition timing requires another change. | A single identified owner/input explains all sampled pose and event transitions. |
| H-PC-010 | Outside-loop timing fields diverge under the full socle; INFERRED from 0x360A4 preceding loop. | If local input returns to A magnitude but outer rate doubles, +0x574 slope doubles in D unless reset/gating changes. | Measured writes/call rate or resets cancel the predicted divergence. This is a field-level test, not automatic gameplay regression. |

Each experiment must state which hypothesis it can reject and which remain observationally equivalent. Prefer the next arm that separates remaining explanations. Preserve failures and null results. Never infer animation semantics from a monotonically increasing float alone.
